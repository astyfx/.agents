__pycache__/
*.pyc
.DS_Store

# Runtime/state directories (local only)
codex/
claude/
migration-backups/

# Cross-session progress scratch file (not committed — use tracking/ for permanent records)
claude-progress.txt
