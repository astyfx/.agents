__pycache__/
*.pyc
.DS_Store

# Runtime/state directories (local only)
codex/
claude/
migration-backups/
.stave/
.skill-lock.json

# Cross-session handoff scratch files (not committed — use execution/ for permanent records)
work-handoff.md
