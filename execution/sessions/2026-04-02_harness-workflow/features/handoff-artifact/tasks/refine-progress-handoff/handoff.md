# Handoff

## Summary

Renamed the primary cross-session scratch artifact to `work-handoff.md`,
reframed the progress-tracker skill around a cleaner handoff format, and updated
the live harness scripts/docs/eval surface to use the new structure.

## Open Issues

No blocking issues. Legacy `claude-progress.txt` references remain only for
compatibility and migration support, and broader cleanup should wait until the
current local change set is committed or merged.

## Next Actions

- Commit or merge the current `work-handoff.md` migration change set.
- After that commit or merge exists, consider repo-by-repo cleanup of legacy
  `claude-progress.txt`.

## Auto Snapshot

TODO: maintained by stop-time automation when available.
